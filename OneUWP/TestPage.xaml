<Page
    x:Class="OneUWP.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:OneUWP.Model"
    
    mc:Ignorable="d">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Name="TestButton" Click="TestButton_Click" Content="开始获取列表"/>
        <TextBlock Grid.Row="1" Name="TestInfoTextBlock"/>
        <ListView Grid.Row="2" ItemsSource="{x:Bind sheet}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Sheet">
                    <StackPanel Orientation="Horizontal" 
                                    Margin="0,10,0,0" 
                                    HorizontalAlignment="Center">
                        <TextBlock Text="{x:Bind id}" Margin="0,0,5,0" />
                       <TextBlock Text="{x:Bind date}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
